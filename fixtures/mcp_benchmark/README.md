# MCP Benchmark Fixtures

Deterministic cases for agent tool-discovery / selection / reject behavior.

See [docs/mcp-benchmark.md](../../docs/mcp-benchmark.md).

```bash
pytest tests/test_mcp_benchmark.py -q
```
